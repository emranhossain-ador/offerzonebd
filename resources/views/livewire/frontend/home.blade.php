<div>

   <main class="wrapper">
        <!-- Hero Section -->
        @include('frontend.components.home.hero-section')

        <!-- Items category section -->
        @include('frontend.components.home.category')

        <!-- Services section -->
        @include('frontend.components.home.services')

        <!-- How it works section -->
        @include('frontend.components.home.how-it-works')

        <!-- Payment Methods section -->
        @include('frontend.components.home.payment-methods')

    </main>

    <!-- footer section -->
    @include('frontend.inc.footer')

</div>
